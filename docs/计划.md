# 招投标知识库应用 — 完整实施计划（v3 · 四层架构）

> **v3 变更：** 从扁平 Phase 列表重组为四层分层架构（应用层 → API 层 → 核心层 → 数据层），应用层从 V2 提前到顶层设计，API 层显式定义。
>
> **零外部 API 依赖。** API 层基于 NiceGUI 自带的 FastAPI，不需要部署任何新服务。

---

## 目录

1. [架构总览](#1-架构总览)
2. [数据层](#2-数据层)
3. [核心层 — 招标知识库引擎](#3-核心层--招标知识库引擎)
4. [API 接口层](#4-api-接口层)
5. [应用层 — 技术标智能编制系统](#5-应用层--技术标智能编制系统)
6. [用户界面（NiceGUI 三面板）](#6-用户界面nicegui-三面板)
7. [实施阶段](#7-实施阶段)
8. [关键技术决策](#8-关键技术决策)
9. [验证方案](#9-验证方案)

---

## 1. 架构总览

```
┌──────────────────────────────────────────────────────────────────┐
│                    应用层：技术标智能编制系统                       │
│  模板匹配 · 大纲生成 · 逐项响应 · docx 导出 · 人工审核流转          │
├──────────────────────────────────────────────────────────────────┤
│                      API 接口层（FastAPI）                        │
│  /analyze  /query  /suggest  /match_templates  /export           │
├──────────────────────────────────────────────────────────────────┤
│                      核心层：招标知识库引擎                         │
│  ┌────────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐          │
│  │ 结构化解析  │ │ 记忆系统  │ │ 规则引擎  │ │ 智能Agent │          │
│  │ 引擎(主)   │ │          │ │          │ │          │          │
│  └────────────┘ └──────────┘ └──────────┘ └──────────┘          │
│  ┌────────────┐ ┌──────────┐                                     │
│  │ RAG 检索    │ │ 生成器    │  ← 降级为语义兜底 + 模板生成        │
│  │ 管线(辅)   │ │ (Ollama) │                                     │
│  └────────────┘ └──────────┘                                     │
├──────────────────────────────────────────────────────────────────┤
│                        数据层                                     │
│  struct.db（结构化）· ChromaDB（向量）· project.db（元数据/记忆/规则）│
└──────────────────────────────────────────────────────────────────┘
```

### 设计原则

| # | 原则 | 说明 |
|:--:|------|------|
| 1 | **结构化为主，RAG 兜底** | 条款号/表格 O(1) 命中；语义搜索只用于模糊自然语言 |
| 2 | **应用层从第一天就定义** | 技术标制作不是事后补丁，是整个系统的目标出口 |
| 3 | **API 层零外部依赖** | 基于 NiceGUI 自带 FastAPI，不部署新服务 |
| 4 | **信息完整性在检索阶段保证** | 表格完整存储，不指望 LLM 拼碎片 |
| 5 | **LLM 只做三件事** | 阅读理解 · 格式化输出 · 按模板生成文本 |

### 应用范围

| 场景 | 说明 | 优先级 |
|------|------|:--:|
| **信息提取** | 从招标文件精准提取条款、表格、要求、评分标准 | 最高 |
| **技术标辅助** | 生成技术标大纲 + 逐项响应模板 + docx 导出 | 最高 |
| 商务标辅助 | 报价相关（后续扩展） | 低 |
| 法律推理 | **明确不做** | ✗ |

> **LLM 能力：** 千问 32B 在结构化素材完备的前提下完全够用。不必在法律推理或 LLM 选型上花时间。

---

## 2. 数据层

### 2.1 存储矩阵

| 数据库 | 技术 | 存储内容 | 定位 |
|------|------|------|:--:|
| `system.db` | SQLite | 项目注册表 + 系统设置 | 全局 |
| `struct.db` | SQLite | 条款树 + 表格库 + 交叉引用索引 + 条款号哈希 | 每项目 |
| `project.db` | SQLite | 文档索引 + 记忆 + 规则 + 会话 + 输出存档 | 每项目 |
| `chroma/` | ChromaDB | 叙述性段落向量（仅语义兜底） | 每项目 |
| `uploads/` | 文件系统 | 上传的原始文件 | 每项目 |

### 2.2 目录结构

```
D:\RAG\rag_system\
├── app.py                       # NiceGUI 主入口
├── main.py                      # CLI 入口（保留）
│
├── data/                        # 数据层
│   ├── system.db
│   └── projects/<uuid>/
│       ├── struct.db
│       ├── chroma/
│       ├── project.db
│       └── uploads/
│
├── core/                        # 核心层 — RAG 管线（语义兜底）
│   ├── config.py
│   ├── document_loader.py
│   ├── chunker.py
│   ├── embedder.py
│   ├── vector_store.py
│   ├── retriever.py
│   ├── reranker.py
│   ├── generator.py
│   ├── ollama_generator.py
│   └── deepseek_generator.py
│
├── struct/                      # 核心层 — 结构化引擎（主）
│   ├── parser/
│   │   ├── docx_parser.py
│   │   ├── clause_extractor.py
│   │   ├── table_extractor.py
│   │   └── xref_extractor.py       # Phase 0b
│   ├── index/
│   │   ├── clause_tree.py
│   │   ├── table_store.py
│   │   ├── xref_index.py           # Phase 0b
│   │   ├── clause_number_index.py
│   │   └── document_hierarchy.py
│   ├── schema.py
│   └── builder.py
│
├── memory/                      # 核心层 — 记忆系统
│   ├── memory_manager.py
│   └── context_collector.py
│
├── rules/                       # 核心层 — 规则引擎
│   ├── rules_engine.py
│   └── rule_converter.py
│
├── agent/                       # 核心层 — 智能体
│   ├── query_planner.py
│   └── search_agent.py
│
├── api/                         # API 层
│   ├── __init__.py
│   ├── routes.py                # 所有端点定义
│   └── schemas.py               # Pydantic 请求/响应模型
│
├── services/                    # 业务编排（连接 API 层 → 核心层）
│   ├── query_service.py
│   ├── ingest_service.py
│   ├── project_service.py
│   └── export_service.py
│
├── app_layer/                   # 应用层 — 技术标编制
│   ├── template_matcher.py
│   ├── outline_generator.py
│   ├── requirement_extractor.py
│   └── docx_exporter.py
│
├── ui/                          # 表现层 — NiceGUI
│   ├── app.py
│   └── components/
│       ├── chat_panel.py
│       ├── project_sidebar.py
│       └── context_panel.py
│
└── models/                      # 本地模型（不变）
```

---

## 3. 核心层 — 招标知识库引擎

> **核心层的每个模块都是独立的 Python 类，API 层直接调用，不依赖 UI。**

### 3.1 结构化解析引擎（主引擎 · Phase 0a 实现）

**职责：** 把 docx 拆成结构化数据，支撑所有精确查询。

**五大产出：**

| 产出 | 存储 | 查询方式 | 应用场景 |
|------|------|------|------|
| 条款树 | `clause_nodes` 表 | 条款号 → O(1)；路径遍历 | "§6.1.16 是什么" |
| 表格库 | `tables` 表 | 表格编号/标题 → O(1) | "危大工程清单" → 完整 A-8 表 |
| 交叉引用索引 | `cross_references` 表 | 正向/反向追踪 | "A-8 和 §6.1.16 的关系" |
| 条款号哈希 | 内存 dict + SQLite | `dict["§6.1.16"]` < 1ms | 精确条款定位 |
| 文档层级视图 | `clause_nodes.path` | 按路径遍历 | "第六章有哪些内容" |

**核心 API：**

```python
class StructuredEngine:
    def get_clause(self, number: str) -> Optional[ClauseNode]: ...
    def get_clause_tree(self, chapter: str) -> List[ClauseNode]: ...
    def get_table(self, number: str) -> Optional[Table]: ...
    def search_tables(self, keyword: str) -> List[Table]: ...
    def get_xrefs_from(self, number: str) -> List[CrossReference]: ...
    def get_document_hierarchy(self) -> DocumentHierarchy: ...
    def search_clauses_by_keyword(self, kw: str) -> List[ClauseNode]: ...
```

**数据库 Schema：**

```sql
-- 条款树
CREATE TABLE clause_nodes (
    id          INTEGER PRIMARY KEY,
    parent_id   INTEGER REFERENCES clause_nodes(id),
    node_type   TEXT CHECK(node_type IN ('volume','part','chapter','section','clause')),
    number      TEXT,            -- "§6.1.16"
    title       TEXT,
    content     TEXT,
    level       INTEGER,
    page_start  INTEGER,
    path        TEXT             -- "/第二卷/第六章/§6.1.16"
);
CREATE INDEX idx_clause_number ON clause_nodes(number);

-- 表格库
CREATE TABLE tables (
    id          INTEGER PRIMARY KEY,
    table_number TEXT,           -- "A-8"
    title       TEXT,
    chapter_ref TEXT,            -- "§6.1.16"
    page_number INTEGER,
    structure   TEXT,            -- JSON：行列结构
    merged_cells TEXT,           -- JSON：合并单元格
    raw_html    TEXT,            -- 完整 HTML 表格
    checkbox_states TEXT,        -- JSON：{"row_col": "checked|unchecked"}
    row_count   INTEGER,
    col_count   INTEGER
);

-- 条款号哈希索引（O(1) 查找）
CREATE TABLE clause_number_index (
    number      TEXT PRIMARY KEY,
    entity_type TEXT CHECK(entity_type IN ('clause','table')),
    entity_id   INTEGER
);

-- 交叉引用（Phase 0b）
CREATE TABLE cross_references (
    id              INTEGER PRIMARY KEY,
    from_clause_id  INTEGER REFERENCES clause_nodes(id),
    from_table_id   INTEGER REFERENCES tables(id),
    to_clause_id    INTEGER REFERENCES clause_nodes(id),
    to_table_id     INTEGER REFERENCES tables(id),
    to_number       TEXT,
    xref_type       TEXT CHECK(xref_type IN ('see','refer_to','according_to','appendix'))
);
```

**表格 JSON 结构（完整保留）：**

```json
{
  "headers": [["序号", "危大工程类别", "范围", "是否涉及"]],
  "rows": [{
    "cells": [
      {"value": "1", "rowspan": 1, "colspan": 1},
      {"value": "基坑工程", "rowspan": 1, "colspan": 1},
      {"value": "开挖深度 ≥ 5m", "rowspan": 1, "colspan": 1},
      {"value": "[✓]", "checkbox": "checked", "rowspan": 1, "colspan": 1}
    ]
  }]
}
```

### 3.2 格式标记识别（Phase 0a 同步实现）

> **职责：** 识别 docx 中的格式标记（红色字体、加粗、高亮等），将格式语义转为数据标记。
> **核心层只提供"能认出格式"的能力；怎么用（红色=重点）由应用层定义。**

```python
class FormatMarker:
    """docx 格式标记提取器"""

    def extract(self, para_element) -> List[FormatTag]:
        """提取段落内的格式标记"""
        tags = []
        for run in para_element.findall('.//w:r', NSMAP):
            props = run.find('w:rPr', NSMAP)
            if props is not None:
                color = props.find('w:color', NSMAP)
                bold = props.find('w:b', NSMAP)
                highlight = props.find('w:highlight', NSMAP)

                tag = FormatTag(
                    text=run.text,
                    color=color.get('{...}val') if color is not None else None,  # "FF0000"
                    bold=bold is not None,
                    highlight=highlight.get('{...}val') if highlight is not None else None,
                )
                if tag.is_formatted:  # 有任何非默认格式
                    tags.append(tag)
        return tags
```

**支持的格式标记：**

| 格式 | XML 元素 | 应用层可用语义 |
|------|------|------|
| 红色字体 | `<w:color w:val="FF0000"/>` | "重点信息，每个标都不同" |
| 加粗 | `<w:b/>` | "关键条款" |
| 高亮 | `<w:highlight w:val="yellow"/>` | "需要注意" |
| 下划线 | `<w:u/>` | "强调" |

### 3.3 RAG 检索管线（辅引擎 · 已有，降级为语义兜底）

**职责：** 仅处理模糊自然语言查询。条款和表格明确不入向量切片。

```python
# 仅以下类型内容进入向量库
VECTORIZED = ["narrative", "preamble", "footnote", "unclassified"]
# 以下明确不入
STRUCTURED = ["clause", "table", "heading"]
```

### 3.4 规则引擎（Phase 2）

**三类管道钩子：**

```
pre-retrieval   → 修改搜索策略（如"危大工程 → 同时查 A-8 + §6.1.16"）
post-retrieval  → 过滤/增强检索结果
post-generation → 格式化答案（来源标注、符号统一）
```

**规则 JSON 示例：**

```json
{
  "name": "危大工程-返回完整表格",
  "rule_type": "return_table",
  "condition": {"keywords": ["危大工程", "危险性较大"]},
  "action": {
    "table_numbers": ["A-8"],
    "also_include_clauses": ["§6.1.16"],
    "xref_depth": 1
  }
}
```

| 规则动作 | 说明 | 示例 |
|------|------|------|
| `return_table` | 直接返回指定完整表格 | "危大工程清单" → A-8 |
| `return_clause` | 直接返回指定条款 | "§6.1.16" → O(1) |
| `source_label` | 自动标注出处 | "来自合同文件 §6.1.16" |
| `checkbox_format` | 统一勾选符号 | `[x]` → `[✓]` |
| `include_always` | 强制包含指定内容 | 回答必须含某条款/表格 |

### 3.5 记忆系统（Phase 2）

```python
class MemoryManager:
    def store(project_id, category, key, value, context=None) -> str: ...
    def get_context_for_query(project_id, query) -> List[dict]: ...
    def promote_to_rule(memory_entry_id) -> str: ...
```

- 四种类别：`preference` | `correction` | `output` | `note`
- 会话级短期 + 跨会话持久化
- 纠错达阈值 N 次 → 自动转规则

### 3.6 智能 Agent（Phase 4）

**职责：** 编排结构化引擎的能力，处理跨模块查询。纯 Python 类，不引入额外 LLM 成本。

```python
class QueryPlanner:
    def plan(self, query: str) -> QueryPlan:
        # 意图分类（LLM 辅助 + 关键词规则兜底）
        intent = self.classifier.classify(query)
        # 按意图路由到结构化引擎的不同方法
        match intent:
            case CLAUSE_LOOKUP → clause_number_index.get(number)
            case TABLE_QUERY  → table_store.search(keywords)
            case XREF_TRACE   → xref_index.trace(number)
            case HIERARCHY    → clause_tree.walk(chapter)
            case SEMANTIC     → vector_store.search(query)  # 兜底
```

**意图分类器（本地正则优先，LLM 仅处理边界）：**

> **设计修正：** 90%+ 的查询可纯本地规则命中（<1ms），不走 LLM。LLM 只处理无法被正则/关键词覆盖的模糊语义查询（~10%）。

```python
class IntentClassifier:
    def classify(self, query: str) -> Intent:
        # 第一层：本地正则 + 关键词（O(1)，零延迟，零 token 消耗）
        intent = self._local_classify(query)
        if intent:
            return intent
        # 第二层：LLM 分类（仅边界情况，~10%）
        return self._llm_classify(query)

    def _local_classify(self, query: str) -> Optional[Intent]:
        q = query.strip()
        # 条款号：§6.1.16 / 第X条 / 6.1.16 / （一）
        if re.search(r'§\s*\d|第\s*\d+[\.\d]*\s*条|（[一二三]+）', q):
            return Intent.CLAUSE_LOOKUP
        # 表格/清单
        if any(kw in q for kw in ["清单", "表格", "有哪些"]):
            return Intent.TABLE_QUERY
        # 交叉引用
        if any(kw in q for kw in ["引用了", "关联", "相关条款", "参见"]):
            return Intent.XREF_TRACE
        # 层级导航
        if re.search(r'第\s*[一二三\d]+\s*[章篇卷]|.*部分.*内容', q):
            return Intent.HIERARCHY_NAV
        return None  # 模糊语义 → LLM 兜底
```

**延迟对比：**

| 查询 | 本地规则 | LLM 分类 | 差距 |
|------|:--:|:--:|:--:|
| "§6.1.16 是什么" | <1ms | ~500ms | **500x** |
| "危大工程有哪些" | <1ms | ~500ms | **500x** |
| "这个项目有什么要求" | 不命中→LLM | ~500ms | — |

**覆盖率预估：** 条款号 40% + 表格 25% + 层级 15% + 交叉引用 10% → 本地覆盖 ~90%。LLM 只处理 ~10% 边界。

---

## 4. API 接口层

> **基于 NiceGUI 自带的 FastAPI。零外部服务依赖。**
>
> `nicegui` 底层就是 FastAPI app，加 REST 端点只需 `@app.api.get(...)` 装饰器。

### 4.1 端点定义

```python
# api/routes.py
from nicegui import app as ng_app

# ── 文档分析 ──

@ng_app.api.post("/api/projects/{project_id}/analyze")
async def analyze_bid_doc(project_id: str, file: UploadFile):
    """上传招标文件 → 结构化解析 → 返回分析摘要"""
    result = await ingest_service.ingest_and_analyze(project_id, file)
    return {
        "document_id": result.doc_id,
        "clause_count": result.clause_count,
        "table_count": result.table_count,
        "key_requirements": result.requirements[:20],  # 前 20 条关键要求
        "scoring_items": result.scoring_items,
        "structure_map": result.hierarchy.to_dict()
    }

# ── 智能问答 ──

@ng_app.api.post("/api/projects/{project_id}/query")
async def query(project_id: str, body: QueryRequest):
    """自然语言查询招标文件"""
    result = await query_service.query(project_id, body.question)
    return {
        "answer": result.answer,
        "sources": [s.to_dict() for s in result.sources],
        "intent": result.intent,
        "path": result.path,           # "structured" | "semantic"
        "applied_rules": result.applied_rules
    }

# ── 投标策略建议 ──

@ng_app.api.get("/api/projects/{project_id}/suggest")
async def suggest_strategy(project_id: str):
    """基于招标文件分析，返回投标策略建议"""
    reqs = await requirement_extractor.extract_all(project_id)
    return {
        "must_respond": reqs.filter(type="must"),
        "scoring_weights": reqs.scoring_summary(),
        "suggested_outline": outline_generator.generate(reqs)
    }

# ── 模板匹配 ──

@ng_app.api.get("/api/projects/{project_id}/templates/match")
async def match_templates(project_id: str):
    """匹配适用的技术标模板"""
    return {
        "templates": template_matcher.find(project_id),
        "match_reasons": template_matcher.explain()
    }

# ── 项目信息 ──

@ng_app.api.get("/api/projects/{project_id}/info")
async def get_project_info(project_id: str):
    """获取项目关键信息提取结果"""
    return {
        "project_name": project.name,
        "bidder": project.bidder_info,
        "deadlines": project.extract_deadlines(),
        "key_clauses": project.get_key_clauses(),
        "document_versions": project.get_version_chain()
    }

# ── 导出 ──

@ng_app.api.post("/api/projects/{project_id}/export")
async def export_tech_bid(project_id: str, body: ExportRequest):
    """导出技术标为 docx"""
    path = await export_service.export_tech_bid(project_id, body.sections)
    return {"download_url": f"/api/files/{path}"}
```

### 4.2 Pydantic 模型

```python
# api/schemas.py
from pydantic import BaseModel

class QueryRequest(BaseModel):
    question: str
    stream: bool = True
    use_agent: bool = False

class QueryResponse(BaseModel):
    answer: str
    sources: List[SourceRef]
    intent: str
    path: str                          # "structured" | "semantic"
    applied_rules: List[str]

class SourceRef(BaseModel):
    clause_number: Optional[str]       # "§6.1.16"
    table_number: Optional[str]        # "A-8"
    volume: Optional[str]
    chapter: Optional[str]
    content_preview: str

class ExportRequest(BaseModel):
    sections: List[str]                # 要导出的技术标章节
    format: str = "docx"
```

### 4.3 两种调用路径（同一套 API）

```
┌─ NiceGUI UI ────────────────────────┐
│  chat_panel.py                      │
│    ↓ 用户点击发送                    │
│  调用 POST /api/query  ←────────────│── 走 HTTP（同机 localhost）
│                                     │
│  或直接 import query_service.query() │── 走 Python 函数调用
└─────────────────────────────────────┘

┌─ 外部系统（未来扩展）────────────────┐
│  OA 系统 / 企业微信机器人            │
│    ↓                                │
│  HTTP POST /api/query               │── 走 REST API
└─────────────────────────────────────┘
```

> **优势：** UI 开发时直接调 Python 函数（零网络开销）。外部集成时同一套 API 走 HTTP。不需要两套代码。

---

## 5. 应用层 — 技术标智能编制系统

> **核心场景闭环：** 读招标文件 → 提取要求 → 生成技术标框架 → 人工填细节 → 导出 docx。
>
> **设计模式：** 每个招标人/行业可挂载自己的领域提取模板，共享同一套核心引擎。新的行业规则只需新增一个提取器文件，不修改核心层。

### 5.0 领域提取器模式（可无限扩展）

```
应用层/app_layer/extractors/
├── __init__.py
├── base_extractor.py           # 提取器基类 + 公共接口
├── gongwushu_extractor.py     # ✨ 深圳市建筑工务署专用
├── jiaotongju_extractor.py    # 将来：XX交通局
└── zhongdian_extractor.py     # 将来：XX中电建
         │
         │  所有提取器实现统一接口：
         │  extract(doc) → ExtractedFields
         ▼
核心层（通用，不变）
```

**工务署提取器（gongwushu_extractor.py）—— 基于用户阅读心得：**

> 工务署招标文件特点是大部分内容同质化。按目录定位关键章节，红色字体标记每个标不同的信息点。
>
> **三条核心修正（来自实施建议）：** (1) 别名层防模板改版——章节名变了照样命中；(2) 白名单优于黑名单——新增非同质化章节不会静默丢失；(3) 本地正则优先——条款号/表格编号 O(1) 命中。

```python
class GongwushuExtractor(BaseExtractor):
    """深圳市建筑工务署招标文件专用提取器"""

    # ── 别名层（防御模板改版）──
    SECTION_ALIASES = {
        "招标公告": ["招标公告", "招标公告（投标邀请书）"],
        "前附表二": ["前附表二", "投标人须知前附表", "前附表（二）"],
        "前附件一": ["前附件一", "工程情况介绍"],
        "第2篇":   ["第2篇", "第二篇", "评标办法"],
        "第3篇":   ["第3篇", "第三篇", "投标文件格式"],
        "第六卷":   ["第六卷", "招标人补充", "招标人修改", "补充修改"],
    }

    # ── 白名单（替代黑名单）──
    EXTRACT_SECTIONS = [
        "招标公告", "前附表二", "前附件一", "第2篇", "第3篇", "第六卷",
    ]
    # 不在白名单的章节 → logger.warning() 告警，提醒显式决策

    def locate_section(self, engine, section_key: str):
        """按别名表定位，任一命中即返回"""
        aliases = self.SECTION_ALIASES.get(section_key, [section_key])
        for alias in aliases:
            node = engine.search_clauses_by_title(alias)
            if node: return node
        return engine.fuzzy_find_section(section_key)

    # ── 字段映射表 ──

    # ── 字段映射表 ──
    FIELD_MAP = {
        # 招标公告
        "标段编号":     {"source": "招标公告", "format": "text"},
        "建设单位":     {"source": "招标公告", "format": "text"},
        "开竣工日期":   {"source": "招标公告", "format": "date_range"},

        # 第一卷·前附表二：工程主要事项（核心字段密集区）
        "工程名称":     {"source": "前附表二", "format": "text"},
        "工程概况":     {"source": "前附表二", "format": "text"},
        "承包范围":     {"source": "前附表二", "format": "text"},
        "招标人另行发包的专业工程": {"source": "前附表二", "format": "list"},
        "甲供材料设备": {"source": "前附表二", "format": "list"},
        "质量标准":     {"source": "前附表二", "format": "text",
                         "ignore_keywords": ["指引文件", "规范性文件"]},
        "质量目标":     {"source": "前附表二", "format": "text"},
        "工期":         {"source": "前附表二", "format": "text"},
        "资质要求":     {"source": "前附表二", "format": "text"},
        "是否接受联合体":{"source": "前附表二", "format": "boolean"},
        "投标文件的组成":{"source": "前附表二", "format": "list"},
        "过多投标人淘汰环节": {"source": "前附表二", "format": "text"},
        "评标方法":     {"source": "前附表二", "format": "text"},
        "评标委员会推荐原则": {"source": "前附表二", "format": "text"},
        "技术标评标地点": {"source": "前附表二", "format": "text"},
        "明暗标规定":   {"source": "前附表二", "format": "text"},
        "定标工作规则": {"source": "前附表二", "format": "text"},
        "定标方法":     {"source": "前附表二", "format": "text"},

        # 前附件一：工程情况介绍
        "工程地址":     {"source": "前附件一", "format": "text"},
        "周边环境":     {"source": "前附件一", "format": "text"},
        "工程主要特点": {"source": "前附件一", "format": "text", "priority": "high"},
        "技术要求":     {"source": "前附件一", "format": "text", "priority": "high"},
        "施工重点和难点":{"source": "前附件一", "format": "text", "priority": "high"},

        # 第2篇·评标办法
        "技术标定性评审表": {"source": "第2篇", "format": "table",
                             "match_rule": "table_id=='J-DX-4'",
                             "note": "可能有多个评审表，以 J-DX-4 为准"},

        # 第3篇·投标文件格式·技术标部分
        "技术标部分目录": {"source": "第3篇", "format": "outline"},
        "承诺函模板":   {"source": "第3篇", "format": "template"},
        "简历表模板":   {"source": "第3篇", "format": "template"},
        "危大工程清单": {"source": "第3篇", "format": "table",
                         "table_id": "A-8"},
        "BIM标定性评审表": {"source": "第3篇", "format": "table"},

        # ✨ 技术标章节清单
        # 来源: 投标人须知 → 投标文件编制 → "10.4技术商务标文件主要包括下列内容"
        # 产出: 10 个章节标题 + 与 J-DX-4 评审项的对应关系
        "技术标章节清单": {
            "source": "第1篇",
            "locate_by": "10.4技术商务标文件主要包括下列内容",
            "format": "chapter_list",        # 编号列表
            "chapter_range": "（1）~（10）",
            "verify_against": {
                "table": "J-DX-4",
                "rule": "章节(1)-(8) = J-DX-4 Row 1-8（评审项）一一对应",
                "confirm_clause": "第1至8章为评标委员会评审内容...J-DX-4",
            },
            "note": "（9）危大工程清单及管理措施 + （10）招标人要求的其他资料 不在 J-DX-4 评审范围内",
            "feeds_into": "outline_generator → 生成技术标目录（来源：招标文件原文，非固定模板）",
        },

        # 第六卷·招标人补充/修改（⚠ 每个标差异的关键，全部内容需识别）
        "招标人补充修改": {"source": "第六卷", "format": "full_text",
                           "match_alt_names": ["招标人.*补充/修改", "招标人.*修改/补充"],
                           "priority": "critical",
                           "note": "可能在别的卷章里，名称模式匹配即可"},
    }

    # ── 优先级规则 ──
    PRIORITY_RULES = [
        # 同一字段多处出现时，哪个为准
        ("工程概况", "前附件一 > 前附表二"),
        ("质量要求", "前附件一 > 前附表二"),
        # 红色字体内容标记为 priority=critical
        ("*", "红色字体 > 普通字体"),  # 由核心层 FormatMarker 提供
    ]

    # ── 同质化章节跳过 ──
    SKIP_SECTIONS = [
        "政府工程廉政警示",
        "反商业贿赂承诺书",
        "深圳市建筑工务署工作人员十不准",
        "深圳市建筑工务署施工现场十项禁令",
        "政府工程投标风险提示",
        "自定义电子招投标系统相关说明",
        "建设工程计算机评标系统相关说明",
        # ... 同质化内容不进入提取管道
    ]
```

**提取器基类：**

```python
class BaseExtractor(ABC):
    """领域提取器基类。每个招标人/行业实现自己的子类。"""

    @abstractmethod
    def get_field_map(self) -> dict:
        """返回字段映射表"""

    @abstractmethod
    def get_priority_rules(self) -> list:
        """返回优先级规则"""

    def extract(self, structured_engine, project_id) -> ExtractedFields:
        """通用提取流程：定位章节 → 提取字段 → 应用优先级 → 返回结构化结果"""
        fields = ExtractedFields()
        for field_name, config in self.get_field_map().items():
            section = self.locate_section(structured_engine, config["source"])
            value = self.extract_field(section, config)
            fields.set(field_name, value)
        self.apply_priority_rules(fields)
        return fields
```

### 5.1 工作流

```
招标文件 docx
    │
    ▼
┌─────────────────────────────────────────────┐
│  Step 1: 需求提取（requirement_extractor）     │
│                                              │
│  结构化引擎扫描招标文件：                        │
│  ├── 提取所有"投标人须/应/必须/不得"条款         │
│  ├── 提取所有评分标准 → 技术标响应权重            │
│  ├── 提取所有附表要求（B-1 到 B-7 等）           │
│  ├── 提取危大工程清单 + 安全管理要求              │
│  └── 提取工期/质量/人员等硬性门槛                │
│                                              │
│  输出：ComplianceMatrix（逐条对照表）            │
└────────────────────┬────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────┐
│  Step 2: 大纲生成（outline_generator）         │
│                                              │
│  基于提取的要求 + 模板匹配，生成技术标大纲：       │
│                                              │
│  §1  工程概况与重难点分析                       │
│       · 来源：投标人须知 §1.1-1.4               │
│       · 需响应：项目规模、地质条件、工期要求       │
│  §2  施工组织设计（→ B-1 表）                   │
│       · 来源：技术规范 §3.x                     │
│       · 需覆盖：9 个要点（见招标文件 B-1）        │
│  §3  施工进度计划（→ B-2/B-3 表）               │
│  ...                                          │
│  §8  安全管理与危大工程专项方案                   │
│       · 来源：§6.1.16 + A-8                     │
│       · 本项目涉及的危大工程：[✓] 基坑/模板/起重...│
│                                              │
│  每个大纲项标注：来源条款号 + 评分权重 + 响应策略   │
└────────────────────┬────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────┐
│  Step 3: 逐项响应（LLM 生成模板）               │
│                                              │
│  对每个大纲项：                                │
│  ┌──────────────────────────────────────┐    │
│  │ 【条款来源】§6.1.16                    │    │
│  │ 【招标要求】[原文自动填入]               │    │
│  │ 【评分权重】5 分                        │    │
│  │ 【响应策略】[LLM 生成的建议思路]          │    │
│  │ 【技术响应】[人工填写区域]               │    │
│  └──────────────────────────────────────┘    │
│                                              │
│  人工逐项填写技术细节                           │
└────────────────────┬────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────┐
│  Step 4: docx 导出（docx_exporter）           │
│                                              │
│  · 生成标准格式的技术标投标文件                  │
│  · 自动插入条款引用脚注                        │
│  · 自动生成 compliance matrix 附录             │
│  · 保留招标文件原文格式兼容                     │
└─────────────────────────────────────────────┘
```

### 5.2 核心数据结构

```python
@dataclass
class RequirementItem:
    """从招标文件提取的单条要求"""
    clause_ref: str              # "§6.1.16"
    requirement_type: str        # "must" | "should" | "scoring" | "attachment"
    content: str                 # 要求原文
    score_weight: Optional[float]

@dataclass
class ComplianceMatrix:
    """投标响应对照表"""
    items: List[RequirementItem]
    total_score: float
    must_count: int
    coverage_pct: float          # 已响应 / 总要求

@dataclass
class TechBidOutline:
    """技术标大纲"""
    sections: List[OutlineSection]
    compliance: ComplianceMatrix

@dataclass
class OutlineSection:
    title: str
    source_clauses: List[str]    # ["§6.1.16", "A-8"]
    score_weight: float
    prompt: str                  # "本节需要描述..."
    ai_template: str             # LLM 生成的响应框架
    user_content: str            # 人工填写
```

### 5.3 模板库

| 模板 | 适用场景 | 来源 |
|------|------|------|
| 施工组织设计 | 所有项目 | B-1 表要求 |
| 危大工程专项方案 | 涉及危大工程的项目 | A-8 + §6.1.16 |
| 质量管理体系 | 所有项目 | B-6 表要求 |
| 安全文明施工 | 所有项目 | §6.1.14 |
| 环境保护方案 | 所有项目 | §6.3 |

---

## 6. 用户界面（NiceGUI 三面板）

```
┌──────────────┬──────────────────────────┬──────────────┐
│  左侧面板    │      中央聊天面板        │  右侧面板    │
│  (280px)     │      (flex)              │  (350px)     │
│              │                          │              │
│ 项目选择框   │  用户: 危大工程有哪些?   │ [结构化命中] │
│ [+新建项目]  │                          │ 表格: A-8   │
│              │  AI: 本项目涉及:         │ 条款: §6.1.16│
│ 📁 深圳中学  │                          │              │
│   📄 招标文件 │  ┌──────────────────┐    │ 来源层级     │
│   📄 补遗001  │  │ A-8 完整表格      │    │ 第二卷       │
│              │  │ 序号│类别│范围│勾选│  │  └ 合同文件  │
│ 技术标       │  │ 1  │基坑│≥3m │[✓]│  │     └ §6.1.16│
│ [生成大纲]   │  └──────────────────┘    │              │
│ [导出 docx]  │                          │ 触发规则     │
│              │  来源: §6.1.16 合同文件   │ · return_table│
│ [上传文件]   │                          │              │
│              │  [纠正] [复制] [导出]    │ 评分权重     │
│              │                          │ 5 分         │
│              │  ┌──────────────────┐    │              │
│              │  │ 输入问题...      │    │ 版本链       │
│              │  └──────────────────┘    │ V3.0→V3.1   │
└──────────────┴──────────────────────────┴──────────────┘
```

**左侧新增：**
- 技术标入口：`[生成大纲]` `[导出 docx]`
- 文档版本链显示

**中央不变：**
- 流式回答（Ollama stream）
- 完整表格内嵌渲染（非碎片）
- 纠正按钮 → 记忆

**右侧增强：**
- 结构化命中标识（表格/条款/交叉引用）
- 评分权重显示

---

## 7. 实施阶段

### 7.1 阶段总览

```
Phase 0a: 最小可用结构化引擎         ← 地基
    │  条款树 + 表格库 + 哈希索引
    │  验证：用深圳中学 EPC 文件跑通
    │
Phase 0b: 增强引擎                   ← 迭代
    │  交叉引用 + 非标准标题 + 合并单元格
    │
Phase 1: 数据层 + API 层 + 项目管理    ← 基础设施
    │  SQLite Schema + REST 端点 + 项目 CRUD
    │
Phase 2: 记忆 + 规则                  ← 智能叠加
    │
Phase 3: 用户界面                     ← 三面板 NiceGUI
    │
Phase 4: 应用层 + Agent + 集成打磨     ← 技术标编制闭环
```

### 7.2 各 Phase 详细

| Phase | 产出 | 代码量 | 验证标准 |
|------|------|:--:|------|
| **0a** | `struct/parser/` + `struct/index/`（除 xref）+ `struct/schema.py` | ~800 行 | `get_clause("§6.1.16")` < 10ms；A-8 表行列/勾选与原文一致 |
| **0b** | `xref_extractor.py` + `xref_index.py` + 条款树增强 | ~600 行 | 正反向引用命中；`trace_xref_chain()` 正确 |
| **1** | `db/` + `api/` + `services/ingest_service.py` | ~500 行 | 上传文件 → struct.db 生成 → `/analyze` 返回正确 |
| **2** | `memory/` + `rules/` | ~400 行 | 纠错记忆 → 自动转规则 → 规则触发正确行为 |
| **3** | `ui/` + `app.py` | ~600 行 | 流式问答 + 表格内嵌渲染 + 技术标入口可用 |
| **4** | `app_layer/` + `agent/` + `export_service.py` | **~1,300 行** | 生成大纲 → 逐项响应 → 导出 docx（docx_exporter 单独 ~500-800 行） |

> **总代码量估算：~3,500 行（不含现有 RAG 管线）**

### 7.3 依赖新增

```
nicegui>=2.0          # UI + FastAPI
pywebview>=5.0        # 桌面打包（可选）
aiosqlite>=0.20       # 异步 SQLite
pyyaml>=6.0           # 规则 YAML
python-docx>=0.8      # 已有
lxml>=4.9             # 已有
```

---

## 8. 关键技术决策

| 决策点 | 选择 | 理由 |
|------|------|------|
| **架构分层** | 四层：应用 → API → 核心 → 数据 | 应用层独立于引擎，API 可被多个前端调用 |
| **检索范式** | 结构化为主，RAG 兜底 | 条款号/表格 O(1)；语义仅用于模糊查询 |
| **API 框架** | NiceGUI 自带 FastAPI | 零额外服务，UI 和 API 共享同一进程 |
| **主存储** | SQLite（结构化）| 条款树/表格库/索引用关系表 |
| **向量存储** | ChromaDB（辅助）| 仅叙述性段落入向量库 |
| **条款号查找** | 内存哈希 + SQLite 持久化 | O(1) < 1ms |
| **表格存储** | 完整 JSON 结构 | 不切片，勾选框/合并单元格完整保留 |
| **LLM** | Ollama qwen3:32b（主）+ DeepSeek API（后备）+ Gemini（备选） | 后备链自动切换：本地→DeepSeek→Gemini；DeepSeek 国内直连 |
| **意图分类** | LLM 快速分类 + 关键词兜底 | 纯正则无法覆盖"危大工程有哪些"等边界 |
| **状态管理** | NiceGUI reactive state | 框架原生 |
| **项目隔离** | 每项目独立 SQLite + ChromaDB 文件夹 | 删除=删文件夹，备份=复制文件夹 |
| **版本管理** | documents 表 version + parent_doc_id | V1 预留字段，V2 实现版本链 UI |
| **法律推理** | **不做** | 专注信息提取 + 技术标制作 |
| **向后兼容** | 保留 `main.py` CLI | 脚本化场景 |

---

## 9. 验证方案

| 阶段 | 测试内容 | 通过标准 |
|------|------|------|
| **0a** | 上传深圳中学 EPC 招标文件 → 验证条款树 → 验证 A-8 表完整提取（含 [✓]/[ ]）→ 验证 `get_clause("§6.1.16")` | 条款命中 < 10ms；表格行列/勾选与原文一致 |
| **0b** | 查询"§6.1.16 引用了哪些" → 验证 A-8 关联 | 正反向索引均命中 |
| **1** | `POST /analyze` → 验证 struct.db 生成 → 验证 ChromaDB 仅含叙述性段落 | struct.db 中 tables.count > 0；向量库无表格碎片 |
| **2** | 存入纠错 → 规则触发 return_table → 验证直接返回完整表格 | 表格未被切片，勾选可见 |
| **3** | "§6.1.16" → < 100ms 响应 → 右侧面板显示条款层级 | 表格内嵌渲染于聊天中 |
| **3b** | "危大工程清单" → 完整 A-8 表格内嵌于聊天回答中 | 表格原文渲染，非碎片拼接 |
| **4** | 点击"生成技术标大纲" → 验证逐条引用正确 → 导出 docx → 用 Word 打开验证 | 大纲章节与招标要求一一对应 |
| **集成** | `python app.py` + `native=True` → 完整流程 | 桌面窗口正常，结构化内容渲染正确 |

---

*v3 · 2026-06-19 · 四层架构：应用层 → API 层 → 核心层 → 数据层*
