# RAG 系统核心依赖

# 向量数据库
chromadb>=1.0.15

# Google Gemini API (嵌入 + 生成)
google-genai>=1.24.0

# 环境变量管理
python-dotenv>=1.1.1

# === 可选依赖 ===
# 如果使用 embedding_provider="local" 或 rerank_provider="local"，还需安装:
# pip install sentence-transformers>=5.0.0
